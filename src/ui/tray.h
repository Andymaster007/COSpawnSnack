#pragma once

// Placeholder for a system tray icon + context menu.
// The MVP runs as a background process; a tray menu should be added so the user
// can start/stop/calibrate without a console window.
namespace csn {
class TrayIcon {};
} // namespace csn
